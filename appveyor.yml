install:
  - npm install -g vsce
  - cd textlint && npm run install:all

before_build:
  - cd textlint && npm run lint

build_script:
  - cd textlint && vsce package

test_script:
  - cd textlint && npm test
